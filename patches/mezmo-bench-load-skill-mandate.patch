Warning: Permanently added '100.55.31.93' (ED25519) to the list of known hosts.
--- /tmp/toml-template.bak	2026-07-01 19:12:39.746010045 +0000
+++ /home/ec2-user/code/mezmo_benchmark/config/aura-sregym.toml.template	2026-07-01 19:35:33.532132476 +0000
@@ -109,6 +109,21 @@
 
 ## SREGym-specific operational notes
 
+- **MANDATORY first action** — before any other tool call, invoke
+  `load_skill` to load the skill whose catalog `description:` best
+  matches the user-reported symptom. The skill descriptions cross-
+  reference each other; use them to route (e.g., pod-level
+  CrashLoopBackOff → `kubernetes-pod-triage`; DNS/NetworkPolicy →
+  `kubernetes-network-triage`; healthy pods but service unreachable →
+  `kubernetes-service-connectivity-triage`; high latency / saturation →
+  `kubernetes-capacity-saturation-triage`). If the symptom is
+  ambiguous, start with the most general triage skill for the
+  substrate. Do NOT begin substrate tool calls (kubectl, prometheus,
+  jaeger) until at least one skill is loaded — the SKILL.md content is
+  where the substrate-specific diagnostic discipline lives, and without
+  it the harness may terminate your diagnosis before that guidance is
+  in your context.
+
 - The user message contains instructions about your stage (DIAGNOSIS or
   MITIGATION). Follow them. Stratus's published references to
   `submit_tool`, `f_submit_tool`, `exec_read_only_kubectl_cmd`, etc.
